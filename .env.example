# ============================================================
# Yishun Again — environment variable reference
# Copy to .env and fill in real values. Never commit .env.
# ============================================================

# ------------------------------------------------------------
# Supabase
# PUBLISHABLE_KEY: safe for frontend (respects RLS)
# SECRET_KEY: bypasses RLS — server / agents only, never expose to browser
# ------------------------------------------------------------
SUPABASE_URL=https://<project-id>.supabase.co
SUPABASE_PUBLISHABLE_KEY=sb_publishable_...
SUPABASE_SECRET_KEY=sb_secret_...

# In Next.js, expose PUBLISHABLE_KEY to the browser via:
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_...

# ------------------------------------------------------------
# Anthropic (Stage 2 writer — claude-haiku-4-5-20251001 / claude-sonnet-4-6)
# Server / agents only. Never in frontend code.
# ------------------------------------------------------------
ANTHROPIC_API_KEY=sk-ant-...

# Stage 2 WRITE model. Moved from claude-sonnet-4-6 to Haiku after the eval in
# packages/agents/tools/eval_l2_write.py over 30 real inputs (15 single-source,
# 15 clustered with 3-9 sources): Haiku matched Sonnet on the gate metric
# (ungrounded numbers / proper nouns) on the multi-source half and on format
# compliance, while running ~24% shorter. Env-overridable so a rollback is a
# config change, not a redeploy.
STAGE2_WRITE_MODEL=claude-haiku-4-5-20251001

# Summary length is arithmetic, not an instruction the model self-polices:
#   budget = clamp(400, 1600, STAGE2_SUMMARY_RATIO x non-signal source chars)
# The prose "~1600 char" ceiling was being ignored — Sonnet exceeded it on 10 of
# 29 eval drafts (worst 2765). 0.75 is the p75 of the measured summary-to-source
# ratio for SINGLE-source stories (median 0.612); multi-source sits near 0.104,
# so any real cluster still saturates the 1600 ceiling. The single-source end is
# what binds: it stops a thin story sprawling without starving a big one.
STAGE2_SUMMARY_RATIO=0.75

# ------------------------------------------------------------
# Model output caps (docs/AUTONOMY.md §5c)
#
# Every JSON-parsing call goes through filters/model_call, which raises a named
# TruncatedResponse on stop_reason='max_tokens' instead of letting a half-object
# surface as "No JSON object in model response". Recovery is automatic — one
# retry at double — and these vars are the manual lever if a cap needs raising
# permanently, without a redeploy.
#
# Measured headroom on the largest real inputs: write 763/2048, classify
# 167/512, batch judge 128/1024, grouper 132/1024. Nothing is near its cap.
# ------------------------------------------------------------
STAGE2_WRITE_MAX_TOKENS=2048
STAGE2_CLASSIFY_MAX_TOKENS=512
CONSOLIDATION_BATCH_MAX_TOKENS=1024
CONSOLIDATION_PAIR_MAX_TOKENS=400
CLUSTER_GROUPER_MAX_TOKENS=1024

# ------------------------------------------------------------
# Gemini (Stage 1 filter — gemini-3.1-flash-lite)
# Server / agents only. Get a key: https://aistudio.google.com/apikey
#
# Replaced Groq in 2026-07 (Developer tier suspended indefinitely; the 6k TPM
# free tier dropped ~33% of a backfill pass to 429s).
#
# STAGE1_MODEL — override to swap models without a code change.
# STAGE1_RPM / STAGE1_RPD — free-tier limits (30 RPM / 1500 RPD). These are
#   ADVISORY defaults: the real cap varies by region, account age and billing,
#   and Google publishes it only in AI Studio, not the docs. A real 429 is the
#   ground truth. Check yours: https://aistudio.google.com/rate-limit
#   RPD quota resets at midnight US/Pacific (NOT SGT).
#
# These must ALSO be set on Cloud Run (agents backend) — see infra/cloudbuild.yaml.
# Not needed in Vercel: the frontends never call Stage 1.
# ------------------------------------------------------------
GEMINI_API_KEY=
STAGE1_MODEL=gemini-3.1-flash-lite
STAGE1_RPM=30
STAGE1_RPD=1500

# ------------------------------------------------------------
# Cloudflare R2 (image storage)
# Bucket name: yishun-assets
# Public custom domain: assets.yishunagain.com
#
# To get these values:
#   1. Cloudflare dashboard → R2 → Manage R2 API tokens
#   2. Create token with Object Read & Write on bucket yishun-assets
#   3. CF_R2_ACCOUNT_ID is your Cloudflare account ID (top-right of dashboard)
# ------------------------------------------------------------
CF_R2_ACCOUNT_ID=
CF_R2_ACCESS_KEY_ID=
CF_R2_SECRET_ACCESS_KEY=
CF_R2_BUCKET_NAME=yishun-assets

# ------------------------------------------------------------
# Next.js ISR (Incremental Static Regeneration)
# REVALIDATE_SECRET: used by apps/web /api/revalidate for on-demand cache
# invalidation. Generate: openssl rand -base64 32
#
# Needed by BOTH Vercel projects, byte-identical:
#   apps/web       — validates the header (timingSafeEqual, exact bytes)
#   apps/war-room  — the rectification route calls it after writing
#                    pixel_art_url (Track B B4b). Unset there and the operator
#                    fixes an image while the live page keeps serving the
#                    placeholder for up to an hour. A mismatch answers 401,
#                    which is deliberately indistinguishable from an attack.
# ------------------------------------------------------------
REVALIDATE_SECRET=

# ------------------------------------------------------------
# Cloudflare Stream (Phase 2 only — leave blank for Phase 1)
# ------------------------------------------------------------
CF_STREAM_TOKEN=

# ------------------------------------------------------------
# MapLibre — OpenFreeMap Liberty style
# No API key, no domain registration, no rate limits.
# https://openfreemap.org
# ------------------------------------------------------------
NEXT_PUBLIC_MAPLIBRE_STYLE=https://tiles.openfreemap.org/styles/liberty

# ------------------------------------------------------------
# Modal.run (SDXL pixel art generation) — DEAD, pending removal in Track B B7.
# The SDXL/Modal/LoRA pipeline was removed July 2026; see docs/ART_PIPELINE.md
# §7. These two vars have no reader left in the codebase.
# ------------------------------------------------------------
MODAL_TOKEN_ID=
MODAL_TOKEN_SECRET=

# ── Art pipeline ───────────────────────────────────────────────────────────
# NEVER hardcode. Google has retired models ahead of published dates.
# Fallback if aspect-ratio control is unavailable: gemini-3.1-flash-image
IMAGE_MODEL=gemini-3.1-flash-lite-image
# Batch mode halves cost ($0.0168 vs $0.0336) but turnaround is up to 24 HOURS,
# so it cannot back an approve click or a publish-time render. Scope it to the
# bulk archive backfill only — never the interactive path. See
# docs/EDGE_CASES_AND_HARDENING.md §1.1. At ~5 images/day the standard-tier
# premium is ~$2.50/month.
IMAGE_USE_BATCH=false
IMAGE_WIDTH=1200
IMAGE_HEIGHT=630

# Master switch for the agents backend. Defaults OFF so an unconfigured deploy
# publishes exactly as it does today rather than logging a failure per incident.
# Needs CF_R2_* + GEMINI_API_KEY + IMAGE_MODEL to be set before turning on.
ART_GENERATION_ENABLED=false

# Public base for generated images. The URL also carries a ?v={md5[:8]} content
# hash — the R2 key is stable so a regeneration overwrites in place, and without
# the hash a re-render is invisible behind the one-year max-age.
R2_PUBLIC_BASE=https://assets.yishunagain.com

# Prompt-writer (Haiku). Scene text only; style/composition/palette/exclusions
# are template constants — see docs/ART_PIPELINE.md §3.8.
IMAGE_SCENE_MODEL=claude-haiku-4-5-20251001
# 0.25, not 0.7: at 0.7 the same incident wandered between mid-morning and late
# afternoon across runs. Not 0 either, or every card reads stamped from one
# template. See ART_PIPELINE.md §3.7.
SCENE_TEMPERATURE=0.25
# Under the floor the scene is rewritten ONCE then accepted — a thin picture
# beats a missing one. The cap is an injection bound; truncation cuts at the
# last sentence end.
SCENE_MIN_CHARS=560
SCENE_MAX_CHARS=1500

# Hard timeout on the Gemini image call. This path blocks an operator's approve
# click, so a hang is worse than a failure.
IMAGE_TIMEOUT_S=30
# Counts ATTEMPTS across a pass, not incidents: the refusal ladder spends up to
# three per incident, so an incident-based cap of 25 would permit 75 calls.
# Reached -> stop generating, publish the rest with null, flag for rectification.
IMAGE_MAX_ATTEMPTS_PER_RUN=40

# ------------------------------------------------------------
# Reddit — no credentials needed.
# Scraper uses the public RSS API (search.rss endpoint).
# No client ID or secret required.
# ------------------------------------------------------------

# ------------------------------------------------------------
# Agents backend ops token (security fix B2)
# All /ops endpoints (pipeline/run, pattern/run, etc.) require this in the
# X-Ops-Token header. Absence → 503; wrong value → 401.
# Generate: openssl rand -base64 32
# Set in Cloud Run: gcloud run services update yishun-agents --set-secrets OPS_TOKEN=ops-token:latest
#
# ALSO needed on the War Room Vercel project, byte-identical: its approve route
# and rectification queue call /art/generate and /art/rectify (Track B B5/B4b).
# Unset there -> every render silently returns 'pending' with no call made.
# ⚠️ The token is not sufficient on its own — Cloud Run is deployed
# --no-allow-unauthenticated, so Google's IAM layer rejects the request with 403
# before the app ever reads the header. See docs/WAR_ROOM_DEPLOY.md.
# ------------------------------------------------------------
OPS_TOKEN=

# ------------------------------------------------------------
# GCP (agents backend — Google Cloud Run)
# Used by gcloud CLI during deploy, not at runtime.
# ------------------------------------------------------------
GCP_PROJECT_ID=yishun-again
GCP_REGION=asia-southeast1

# ------------------------------------------------------------
# Autonomy — daily unattended pass (docs/AUTONOMY.md)
# ------------------------------------------------------------
# Where every operator alert goes: review queue, anomalies, maintenance
# digests, health alarms, monthly reports.
OPERATOR_EMAIL=

# Email transport. Absent => alerts are still RECORDED in the notifications
# table and visible in War Room, but nothing is sent. notify() never fails.
# Get a key: https://resend.com/api-keys
RESEND_API_KEY=
# Sender. resend.dev works with zero DNS setup; switch to a verified
# yishunagain.com sender once the domain is set up in Resend.
NOTIFY_FROM=Yishun Again <onboarding@resend.dev>
# Kill switch for sending, without removing the key.
NOTIFY_ENABLED=true

# Auto-publish gate (req #3). A draft at or above this confidence is published
# with no human review. The four hardcoded legal guardrails still apply and are
# NOT overridable by this value — see docs/AUTONOMY.md.
AUTO_PUBLISH_CONFIDENCE=0.95
# Blast-radius cap per run. Excess stays pending for the operator.
AUTO_PUBLISH_MAX_PER_RUN=25

# Oversized-merge graduation (docs/AUTONOMY.md §5b). A cluster larger than
# CLUSTER_MAX_SIZE is written INTACT as one row and flagged, then held for
# operator review until the grouper has earned merges that size:
#   trusted = decisions >= MIN_SAMPLES and (approvals+1)/(approvals+rejections+2) >= TRUST
# 5 clean approvals lift the hold automatically; one rejection re-arms it.
# Stricter than learning_monitor's 0.70 on purpose — smoothing alone reads 0.75
# after only two approvals, which is not a track record.
OVERSIZED_MERGE_TRUST=0.80
OVERSIZED_MERGE_MIN_SAMPLES=5

# Learning-delta tracking (req #5)
LEARNING_WINDOW_DAYS=30
LEARNING_MIN_SAMPLES=20

# Cost guard (req #12) — the daily estimate that trips a 'health' alert.
COST_ALERT_USD_PER_DAY=2.00

# Per-agent kill switch, comma-separated. Turns an agent off without a
# redeploy. Names: auto_publish, integrity, supervisor, learning_monitor,
# backend_health, maintenance, monthly_report
AGENT_DISABLED=

# Ingestion pass budget in seconds. Must leave room inside the Cloud Run
# request timeout (3600s) for the seven agents that run after it.
INGESTION_MAX_SECONDS=1500

# In-process APScheduler. FALSE in production: Cloud Run scales to zero, so a
# background scheduler needs min-instances=1 + CPU-always-allocated (~$15-25/mo
# for ~15 min of daily work). Production is driven by Cloud Scheduler hitting
# POST /orchestrator/daily. Set true only for local scheduling.
ENABLE_INPROCESS_SCHEDULER=false

# ------------------------------------------------------------
# Story clustering (gather -> cluster -> write-once). docs/INGESTION_DESIGN.md.
# Fixes the single-source-per-incident problem: today N outlets reporting one
# event produce N Sonnet drafts and one single-source incident. Clustering groups
# them so one draft is written with all sources (corroboration_count = N).
#   off    — per-candidate path, byte-identical.  THE CODE DEFAULT.
#   shadow — current path unchanged, but LOGS what clustering would group
#            (validate on live data before it writes anything)
#   on     — write one row per cluster.  Fully wired since 2026-07 (commit
#            1fe8aae). An earlier version of this comment said "not wired yet;
#            treated as shadow" — stale, which is why this block is dated.
#
# ✅ PINNED in infra/cloudbuild.yaml (2026-07-30) via --update-env-vars, so every
# build re-asserts 'on'. Until then it was set only out-of-band on the Cloud Run
# service and appeared nowhere in the repo, while the CODE default is 'off'.
#
# Why it needed pinning when the other new vars did not: 'off' is a VALID mode.
# It raises nothing and logs nothing — the pass just goes back to one queue row
# per URL, and the only symptom is the single-source rate drifting back toward
# its 75.6% baseline over weeks. Every other var added by the 2026-07 programme
# already defaults correctly in code, so pinning those would only create a second
# source of truth to drift.
#
# Still worth knowing: `gcloud run ... --set-env-vars` REPLACES the whole env set,
# so any key omitted from such a command is deleted. Use --update-env-vars to
# change one value. (The pin in cloudbuild.yaml self-heals this on next deploy.)
# ------------------------------------------------------------
CLUSTER_BEFORE_WRITE=on
# Grouping thresholds (biased toward SPLIT — a wrong merge corrupts an incident,
# a wrong split is a cosmetic miss). Stricter than consolidation's overlap=1.
#
# These now gate only which candidates are OFFERED to the batched grouper; one
# Haiku call makes the actual grouping decision. See
# docs/PIPELINE_CHANGES_2026-07-30.md §1.
CLUSTER_MIN_OVERLAP=2
CLUSTER_DATE_WINDOW_DAYS=3
# A cluster larger than this is NO LONGER shredded into one row per member. It is
# written INTACT as one row and flagged for review, and that hold lifts itself
# once the grouper has earned merges this size (docs/AUTONOMY.md §5b).
CLUSTER_MAX_SIZE=6
# RETIRED, kept so an existing deployment config is not an error. It capped
# per-pair merge-confirmation calls; batched grouping makes one call regardless,
# so nothing reads it. Same for CONSOLIDATION_MAX_JUDGEMENTS and
# CONSOLIDATION_EARLY_EXIT_CONFIDENCE.
CLUSTER_MAX_JUDGES=20

# ------------------------------------------------------------
# App URLs
# NEXT_PUBLIC_SITE_URL: public-facing domain — canonical host is WWW.
#   Must match in Vercel env vars (canonical/OG/sitemap URLs are built from it).
#   Also needed on the WAR ROOM project: the rectification route POSTs to
#   {NEXT_PUBLIC_SITE_URL}/api/revalidate. Set to the apex and that POST hits
#   the apex->www redirect, so it MUST carry the www host.
# WAR_ROOM_URL: private subdomain, protected by Cloudflare Access
#               never expose in public DNS or sitemap
# ------------------------------------------------------------
NEXT_PUBLIC_SITE_URL=https://www.yishunagain.com
WAR_ROOM_URL=https://warroom.yishunagain.com

# ------------------------------------------------------------
# War Room -> agents backend (image generation, Track B B5/B4b)
# The approve route and the rectification queue call the Python image generator
# over HTTP — the War Room is TypeScript and cannot import it. Set on the War
# Room's Vercel project only; the agents backend does not read these.
# Pair with OPS_TOKEN above.
# ------------------------------------------------------------
AGENTS_API_URL=https://yishun-agents-xxxxx-as.a.run.app
# Approve-path budget: must exceed the backend's own ladder (3 image calls +
# 3 Haiku rewrites) with margin, or the HTTP layer aborts mid-generation and
# leaves the operator with no feedback and an orphan object in R2.
ART_TIMEOUT_MS=120000
# Rectification is ONE attempt with no ladder, so it needs far less.
RECTIFY_TIMEOUT_MS=45000

# ------------------------------------------------------------
# GitHub (reference — not used at runtime)
# ------------------------------------------------------------
GITHUB_REPO_URL=https://github.com/trolololsaurus/yishun-again
